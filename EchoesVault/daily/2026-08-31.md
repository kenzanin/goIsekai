## Session — 2026-08-31T10:44:17.362Z

## Session wrap-up (2026-08-31, goIsekai)

### Shipped (all committed, tree clean)
1. **Human Verify v1 + WebP cache + per-plugin thumb_ratio** (`1929318`, 24 files) — full OpenSpec change `plugin-verify-webp-thumbs` (18/18 tasks): cookie-paste verify panel in plugins.jet (Open verification page / paste Cookie header / optional browser UA → seeded into tls-client cookie jar with UA override), challenge detection (403/503+marker) → amber banners in search/detail, disk cache converts jpeg/png→.webp quality 85 (gen2brain/webp, pure-Go), MangaDex emits thumb_ratio 0.703 (256/364) with aspect-ratio slots in library/search. Lanes: fix-3/4/5/6 delivered; des-1 empty again → took templates directly.
2. **Logs page copy fix + reader Retry re-fetch** (`1ee1670`) — polling no longer destroys text selection (selectionchange guard + innerHTML swap, badge "Paused — selecting text"), Copy/Clear buttons, ?limit= selector 100–2000 (default 500); reader Retry re-fetches page list preserving current page + drops stale preload.
3. **Image burst pacing — ROOT CAUSE of recurring 404** (`159d2df`) — MD@Home nodes 404 concurrent/rapid bursts (NOT URL expiry; at-home does NOT rotate nodes). Browser draw+prefetch fires parallel /image requests → first 5 "200"s were disk-cache hits, rest 404; client auto-retry amplified the rate limit ("kedap-kedip"). Fix in bridge GetImage: 1-slot semaphore + per-host pacing ≥1.1s (MD@Home convention) + 3x quadratic-backoff retry. Live-verified: 4 parallel fresh fetches → all 200, zero retries.
4. **Favicon + CSS path bug** (`4778bdc`) — manga-book inline SVG favicon (frontend/favicon.svg) linked in both layouts; CRITICAL bonus bug: tailwind regen was writing to frontend/static/lib/ which is NEVER served (routes.go strips /static/ and serves frontend/ directly) — live CSS had been stale since the mark-as-read arc (missing select-text etc.). Fixed output path to frontend/lib/tailwind.css, deleted stray dir.

### Verified facts
- Disk cache confirmed working: cached page served in 0.0009s with zero network fetches (log counter unchanged); fresh page → 200 + written to disk as .webp.
- MangaDex official limits (saved memory #2051): global ~5 req/s per IP (429 → temp 403 ban → drop); GET /at-home/server/{id} 40 req/min; images must be proxied (we do). Our 1.1s pacing is safely under all limits; user approved NOT parsing X-RateLimit-Retry-After (YAGNI).

### Gotchas discovered (not yet in memory system)
- jet v6 has no map index() at runtime → registered global helper `ratioAt(ratios, pluginID)` in engine.go.
- go-jet UPDATE uses col.EQ(); ErrNoRows from jet is qrm.ErrNoRows, not sql.ErrNoRows (GetPluginVerify fixed via errors.Is).
- jetgen duplicate struct collision: local PluginVerify struct clashed with dot-imported generated table type → renamed to PluginVerifyRow.
- curl -w "%{content_type}" typo'd with double braces breaks silently; sed replacement `&` = matched text (mangles URLs containing & — build test URLs in python instead).

### Unresolved / next steps
- None blocking. Optional future: parse X-RateLimit-Retry-After for adaptive backoff (deferred, YAGNI).
- OpenSpec change `plugin-verify-webp-thumbs` is complete but NOT yet archived (/opsx-archive when user wants).


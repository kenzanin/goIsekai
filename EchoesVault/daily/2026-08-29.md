### Scratchpad — 2026-08-29T01:17:09.542Z

**hostnet proxy migrated from net/http to tls-client v1.15.1** (branch `omos/tlsclient`, commit `9bb18af`). `internal/hostnet/proxy.go`: replaced `*http.Client` + `map[*cookiejar.Jar]` with lazily-created `map[string]tls_client.HttpClient` (one per plugin id, guarded by `sync.Mutex`), each built with `WithTimeoutSeconds(30)`, `WithClientProfile(profiles.Chrome_146)`, `WithCookieJar(NewCookieJar())`. `http "github.com/bogdanfinn/fhttp"` for request/response types. `buildHeaders` now assembles the COMPLETE header set (defaults + sorted overrides, empty-Referer skipped) into `req.Header` plus lowercase `http.HeaderOrderKey`, because tls-client fully replaces client defaults when `req.Header` is populated. `flattenHeaders` param is now fhttp `http.Header`. go.mod `go` directive untouched at 1.27. Tests + build + vet all pass with `CGO_ENABLED=0`.


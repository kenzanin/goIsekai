// Package pluginmanager manages the lifecycle of WASM source plugins: discovery,
// loading into the wazero runtime, resource limits, per-call timeouts, and
// panic isolation.
package pluginmanager

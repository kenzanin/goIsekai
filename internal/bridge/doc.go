// Package bridge exposes the reader's core operations to the Wails frontend via
// AppService bindings and serves manga images through a local proxy protocol
// (manga-img://) to bypass CORS and hotlink restrictions.
package bridge
